using UnityEngine;

public class bloquemetal : bloque
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        recistencia = 5;
    }

    // Update is called once per frame
   
}
